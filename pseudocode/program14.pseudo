DECLARE Count : INTEGER
DECLARE Flag : BOOLEAN
Count ← 1
Flag ← FALSE
WHILE Flag = FALSE AND Count <= 5 DO    
    CALL ReBoot()
    Count ← Count + 1
    Flag = Check()
ENDWHILE
IF Flag = FALSE
    THEN
        Alert(27)
ENDIF

